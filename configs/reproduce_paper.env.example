# Compatibility entry: the actual dataset/method matrix lives in JSON.
# Copy configs/method_experiments.json.example to a private location and edit it.
EXPERIMENT_CONFIG=/path/to/private_method_experiments.json
PYTHON_BIN=python3
# Prints a plan by default:
# bash scripts/reproduce_paper.sh
# Explicit execution into a fresh output_root:
# bash scripts/reproduce_paper.sh --execute
